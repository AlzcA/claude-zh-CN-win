# 2026-04-10 工作日志

## 项目: cc-i18n Windows 支持二创

将 claude-code-i18n 工具从仅支持 Linux 修改为支持 Windows 系统。

### 修改内容

1. **finder.ts** - 添加 Windows 平台 Claude Code 安装路径查找
   - 添加 `findInPath()` 跨平台命令查找函数
   - 添加 Windows 常见安装路径 (npm/pnpm/yarn global, Volta, Scoop, Chocolatey, Winget 等)
   - 添加 Windows 原生安装程序路径 (~/.claude, AppData, Program Files 等)

2. **patcher.ts** - 状态目录路径跨平台兼容
   - 添加 Windows 特定的 `validateSyntax()` 处理

3. **backup.ts** - 备份文件后缀跨平台兼容

4. **install-wrapper.ts** - 添加 Windows 包装脚本
   - 创建 PowerShell/Batch 包装脚本 (`claude.bat`)
   - 自动创建 `claude.cmd` 作为辅助入口
   - 提供 Windows 特定的 PATH 配置说明

5. **watcher.ts** - 添加 Windows 自动检查更新 hook
   - 生成 PowerShell 版本的自动检查函数

6. **detector.ts** - 添加 Windows 语言检测
   - 添加 Windows locale 代码映射
   - 支持通过 PowerShell 获取系统 UI 语言

7. **plugin-installer.ts** - 跨平台 Claude 配置目录

8. **README.zh-CN.md** - 添加 Windows 使用说明
   - 添加 Windows PowerShell 快速开始指南
   - 添加 Windows 自动重装 wrapper 说明

9. **patcher.ts** - 补充 zh-CN postPatchRules（2026-04-10 23:58 完成）
   - 新增启动引导界面翻译："Ask Claude to create..." → "让 Claude 帮你创建新项目..."
   - 新增启动引导界面翻译："Run /init to create a CLAUDE.md..." → "运行 /init 为 Claude 创建 CLAUDE.md..."
   - 新增主目录警告翻译："Note: You have launched claude in your home directory..." → "注意：你在主目录启动了 Claude..."
   - 同步补充了与 zh-TW 规则对齐的所有其他 zh-CN 翻译条目（技能描述、设置说明、背景任务、统计页面、代理状态等约 50 条规则）
   - 构建验证通过，npx tsx src/cli.ts status 正常运行
   - 用户需手动运行 `npx tsx src/cli.ts patch` 使新翻译生效

10. **patcher.ts** - 补充快捷键提示翻译（2026-04-11 00:15 完成）
    - `? for shortcuts` → `？ 对于快捷方式`
    - `/ for commands` → `/ 查看命令列表`
    - `/btw for side question` → `/btw 附加提问`
    - `to undo` → `撤销`
    - `to auto-accept edits` → `接受编辑建议`
    - `for verbose output` → `详细输出`
    - `to toggle tasks` → `切换任务`
    - `in $EDITOR` → `在 $EDITOR 中编辑`
    - `/keybindings to customize` → `/keybindings 自定义快捷键`
    - 以及其他已有的中文提示规范化（如 Ctrl+Z、ESC 等大小写）

11. **zh-CN.json** - 修复所有繁體字为简体
    - 修复了浏览器自动化相关描述（约 30+ 条）：`应用程式`→`应用程序`、`視窗`→`窗口`、`雙击`→`双击`、`無障礙`→`无障碍`等
    - 修复了 CLI 工具描述：`预设`→`预设`、`丛集`→`集群`、`型别`→`类型`、`档案`→`文件`等
    - 修复了 `账号狀態`→`账号状态`、`來`→`来`、`請`→`请` 等零散繁體
    - 构建验证通过，JSON 语法正确